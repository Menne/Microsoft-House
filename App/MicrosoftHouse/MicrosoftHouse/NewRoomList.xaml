<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
			 xmlns:local="clr-namespace:MicrosoftHouse;assembly=MicrosoftHouse"
			 x:Class="MicrosoftHouse.NewRoomList">
	<ContentPage.Content>
		<ListView ItemsSource="{x:Static local:AllRooms.All}"
				  HasUnevenRows="true"
				  ItemTapped="OnNewRoom">
			<ListView.ItemTemplate>
				<DataTemplate>
					<ViewCell>
						<ContentView Padding="10">
							<StackLayout Orientation="Horizontal">
								<BoxView x:Name="boxView"
										 Color="{Binding Available}"
										 WidthRequest="20"
										 HeightRequest="20"/>
								<StackLayout>
									<Label Text="{Binding Name}"
										   FontSize="22"
										   VerticalOptions="StartAndExpand" />
									<Label Text="{Binding Piano, StringFormat='Piano = {0}'}"
										   FontSize="16"
										   VerticalOptions="CenterAndExpand" />
								</StackLayout>
							</StackLayout>
						</ContentView>
						<ViewCell.ContextActions>
							<!-- Implementare il Comdando -->
						</ViewCell.ContextActions>
					</ViewCell>
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>
	</ContentPage.Content>
</ContentPage>
